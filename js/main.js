document.addEventListener('DOMContentLoaded', () => {
    makeActivityCard(newActivity);
    makeCounter();
    showActivityModal(newActivity);
    makeEllipseText();
    showTourismCard();
    backToTop();
})